let width = Number(prompt("Width"));
let length = Number (prompt("Length"));

alert(calcsurface(width,length));

// Multiply 2 numbers together
// @param {number} a The first number
// @param {number} b The second number
// @return {number} The total of the multiplication

function calcsurface(a,b){
   return a*b;
}

